# Proportions may be represented with integers only

Suppose `a = d/f` and `b = g/h`; `f != 0`; `h != 0`; `d`, `f`, `g`, `h` are integers.

Then `a:b = (d/f):(g/h) = dh:fg`.

Since `d` and `h` are integers, their product is also and integer. Likewise for `f` and `g`.
